<Window x:Class="WorldClockWidget.AddCityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add a city" Height="420" Width="360"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBox x:Name="SearchBox" TextChanged="SearchBox_TextChanged"/>
            <TextBlock x:Name="HintText" Text="Loading city database..."
                       Foreground="Gray" FontSize="11" Margin="2,4,0,0"/>
        </StackPanel>
        <ListBox x:Name="ZoneList" Grid.Row="1" Margin="0,8,0,0" DisplayMemberPath="Label" MouseDoubleClick="ZoneList_MouseDoubleClick"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Cancel" Width="80" Margin="0,0,8,0" Click="CancelButton_Click"/>
            <Button Content="Add" Width="80" Click="AddButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
