<Window x:Class="WorldClockWidget.AddCityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add a city" Height="420" Width="360"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="SearchBox" Grid.Row="0" Margin="0,0,0,8" TextChanged="SearchBox_TextChanged"/>
        <ListBox x:Name="ZoneList" Grid.Row="1" DisplayMemberPath="DisplayName" MouseDoubleClick="ZoneList_MouseDoubleClick"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Cancel" Width="80" Margin="0,0,8,0" Click="CancelButton_Click"/>
            <Button Content="Add" Width="80" Click="AddButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
